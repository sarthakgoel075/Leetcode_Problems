# Write your MySQL query statement below
select customer_id,COUNT(visit_id) as count_no_trans from Visits as v where NOT EXISTS (select 1 from  Transactions t where v.visit_id = t.visit_id) GROUP BY customer_id ORDER BY count_no_trans